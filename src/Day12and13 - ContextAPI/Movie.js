import React from "react";

const Movie = () => {
  return (
    <div>
      {/* {movies.map((movie, index) => {
        return <h3 key={index}>{movie.name}</h3>;
      })} */}
    </div>
  );
};

export default Movie;
